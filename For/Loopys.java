package For;
import java.util.Scanner;



/**
 * Write a description of class Loopys here.
 *
 * @author aren
 * @version 11.4.24
 */
public class Loopys
{
    // instance variables - replace the example below with your own
    private int words;

    /**
     * Constructor for objects of class Loopys
     */
    public Loopys()
    {
        // initialise instance variables
         //System.out.println("How many times would you like this printed?");
       //Scanner scan =new Scanner(System.in);
       //int word=scan.nextInt();
       //Scanner scan2 =new Scanner(System.in);
       //String word2=scan.nextLine();
       //System.out.println("What would you like to print?");
       //Scanner scan4 =new Scanner(System.in);
       //String word4=scan.nextLine();
        boolean writing= true;
        int number = 0;
        int divide= number % 7;
        if (writing = true)
        number++;
        while (divide == 7)
        System.out.println(number);
            //words++;S
            //if (words < ) System.out.print(number);
            //if (words < word) System.out.print(": ");
            //if (words < word)
            //System.out.println(word4);
            if (number > 100)
            writing=false;
        }//while (writing);
    }

